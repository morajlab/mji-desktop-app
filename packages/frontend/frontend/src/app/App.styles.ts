import { createStyles } from '@mantine/core';

export const Styles = createStyles(() => ({
  launcher: {
    inset: 0,
    top: 'auto',
    bottom: '20px',
    position: 'absolute',
    padding: '20px',
  },
}));
